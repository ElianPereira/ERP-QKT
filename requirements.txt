# Dependencias directas del proyecto, sin pinnear versión exacta (eso lo
# hace requirements.lock). Dockerfile y CI instalan desde requirements.lock,
# no desde este archivo — es la fuente de la que se genera, no lo que se
# despliega. Al cambiar algo aquí, regenerar el lock:
#   pip install pip-tools
#   pip-compile requirements.txt --output-file=requirements.lock --resolver=backtracking
Django>=6.0
gunicorn
psycopg2-binary
dj-database-url
whitenoise
python-decouple
django-jazzmin
pandas
openpyxl
weasyprint
django-anymail
django-storages
boto3
# django-cloudinary-storage y cloudinary ya no son el storage activo (ver
# STORAGES en core_erp/settings.py), pero deben permanecer instalados:
# varias migraciones históricas de comercial/facturacion importan
# cloudinary_storage.storage a nivel de módulo y Django necesita poder
# cargarlas para construir el grafo de migraciones.
django-cloudinary-storage
cloudinary
requests
urllib3
python-docx
pdfplumber
markdown
nh3
defusedxml
